// includes/loader for loading elm through Main.elm.
div#elm-main

script.
  document.addEventListener(
    "DOMContentLoaded",
    () => {
        const node = document.getElementById("elm-main")
        Elm.Main.embed(node)
    }
  )
